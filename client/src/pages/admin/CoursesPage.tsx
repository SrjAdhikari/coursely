//* src/pages/admin/CoursesPage.tsx

/** Placeholder — replaced in Task 9 (Courses list). */
const CoursesPage = () => (
	<h1 className="font-heading text-3xl font-semibold">Courses</h1>
);

export default CoursesPage;
