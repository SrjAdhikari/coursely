//* src/pages/admin/OverviewPage.tsx

/** Placeholder — replaced in Task 9 (Overview). */
const OverviewPage = () => (
	<h1 className="font-heading text-3xl font-semibold">Overview</h1>
);

export default OverviewPage;
